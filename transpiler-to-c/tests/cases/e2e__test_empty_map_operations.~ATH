
        import timer T(1ms);
        ~ATH(T) { } EXECUTE(
            BIRTH m WITH {};
            UTTER(LENGTH(KEYS(m)));
            m = SET(m, "a", 1);
            UTTER(LENGTH(KEYS(m)));
        );
        THIS.DIE();
    