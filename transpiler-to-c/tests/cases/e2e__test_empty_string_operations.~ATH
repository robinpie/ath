
        import timer T(1ms);
        ~ATH(T) { } EXECUTE(
            BIRTH s WITH "";
            UTTER(LENGTH(s));
            s = s + "hello";
            UTTER(LENGTH(s));
        );
        THIS.DIE();
    