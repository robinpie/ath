
        import timer T(1ms);
        ~ATH(T) { } EXECUTE(
            BIRTH called WITH DEAD;
            RITE setTrue() {
                called = ALIVE;
                BEQUEATH ALIVE;
            }
            BIRTH x WITH ALIVE OR setTrue();
            SHOULD called {
                UTTER("called");
            } LEST {
                UTTER("not called");
            }
        );
        THIS.DIE();
    